# Render compatibility launcher

The existing Render service is configured with `autovyne` as its Root Directory.
This package installs the application's dependencies in that directory and launches
the source files stored at the repository root.

After the Render service Root Directory is changed to blank, this compatibility
directory can be removed.
