<!-- TESTIMONIALS SECTION -->
<section class="testimonials-section">
  <div class="testimonials-inner">
    <div class="testimonials-header">
      <div class="testimonials-label">Real Results</div>
      <h2>What Business Owners Are Saying</h2>
    </div>
    <div class="testimonials-grid">
      <div class="testimonial-card">
        <div class="testimonial-quote">"We were missing 6 calls a week after hours. Autovyne captured every single one. $18K in new revenue in the first 3 months."</div>
        <div class="testimonial-attr">
          <span class="testimonial-name">Brandon</span>
          <span class="testimonial-sep">—</span>
          <span class="testimonial-role">Owner, Independence HVAC</span>
        </div>
      </div>
      <div class="testimonial-card">
        <div class="testimonial-quote">"I was skeptical at first. 90 days in, I've got 14 new jobs I wouldn't have gotten. Worth every penny."</div>
        <div class="testimonial-attr">
          <span class="testimonial-name">Mark</span>
          <span class="testimonial-sep">—</span>
          <span class="testimonial-role">Owner, Overland Park Plumbing</span>
        </div>
      </div>
      <div class="testimonial-card">
        <div class="testimonial-quote">"Setup was 20 minutes. First missed call captured that night. It's been running perfectly since."</div>
        <div class="testimonial-attr">
          <span class="testimonial-name">Tina</span>
          <span class="testimonial-sep">—</span>
          <span class="testimonial-role">Owner, KC Metro Electric</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
/* ---- TESTIMONIALS ---- */
.testimonials-section {
  padding: 100px 24px;
  background: var(--bg-subtle);
}
.testimonials-inner {
  max-width: var(--max-w);
  margin: 0 auto;
}
.testimonials-header {
  text-align: center;
  margin-bottom: 56px;
}
.testimonials-label {
  display: inline-block;
  font-family: var(--font-display);
  font-size: 0.78rem;
  font-weight: 600;
  letter-spacing: 1.5px;
  text-transform: uppercase;
  color: var(--accent);
  margin-bottom: 16px;
}
.testimonials-header h2 {
  font-family: var(--font-display);
  font-size: clamp(1.8rem, 3.5vw, 2.6rem);
  font-weight: 700;
  letter-spacing: -0.01em;
}
.testimonials-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
}
.testimonial-card {
  background: var(--bg-card);
  border: 1px solid rgba(255,255,255,0.07);
  border-radius: 16px;
  padding: 36px 32px;
  display: flex;
  flex-direction: column;
  gap: 20px;
  transition: border-color 0.25s ease, transform 0.2s ease;
}
.testimonial-card:hover {
  border-color: rgba(0,232,123,0.2);
  transform: translateY(-3px);
}
.testimonial-quote {
  font-size: 1rem;
  line-height: 1.7;
  color: var(--fg);
  font-style: normal;
  flex: 1;
}
.testimonial-quote::before {
  content: '\u201C';
  display: block;
  font-family: var(--font-display);
  font-size: 3rem;
  font-weight: 700;
  color: var(--accent);
  opacity: 0.4;
  line-height: 1;
  margin-bottom: 8px;
}
.testimonial-attr {
  display: flex;
  align-items: center;
  gap: 6px;
  flex-wrap: wrap;
  padding-top: 16px;
  border-top: 1px solid rgba(255,255,255,0.07);
}
.testimonial-name {
  font-family: var(--font-display);
  font-size: 0.9rem;
  font-weight: 700;
  color: var(--fg);
}
.testimonial-sep {
  color: var(--fg-muted);
  font-size: 0.85rem;
}
.testimonial-role {
  font-size: 0.85rem;
  color: var(--fg-muted);
}
@media (max-width: 900px) {
  .testimonials-grid { grid-template-columns: 1fr; }
  .testimonials-section { padding: 72px 20px; }
}
</style>